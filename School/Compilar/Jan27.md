# January 27, 2026



When making a compiler, it should be ambigous in the text that is used to keep everything simplified.

When making a compiler, it should be very simple. Meaning it should be fast.

The word **lexonizing** is parsing the word letter by letter

When it comes to a **word**, this should consume the maximum amount of characters that are possible. For example, if the word "variable" is used, then it should consume the maximum amount of characters and not just stop at something like "var" and it works.

**Binding** is associated





<leader> + f --> format code

<leader> + s --> will do global replace on keyword that cursor is currently on

<leader> + x --> makes current file being edited into an executable. Gains the "x" permission

<leader> +  --> 

<leader> +  --> 

<leader> +  --> 

<leader> +  --> 

<leader> +  --> 

<leader> +  --> 





In iTerm2 terminal, do cmd+shift+. to open AI prompt to ask question about CLI related things like "how to show files"



